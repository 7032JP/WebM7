# 更新履歴

| 日付 | 内容 |
|------|------|
| 2026-09-21 | **スマートフォン縦表示の改善**<br>・スマートフォンの縦向き表示で、画面、ソフトウェアキーボード、本体（フロッピィ・電源など）の順に並べ、画面とキーボードをスクロールせずに 1 画面で操作できるようにしました。<br>・ブラウザの開発者ツールの端末模擬など、タッチ端末と判定されない環境でも、縦向きの狭い画面ではソフトウェアキーボードを既定で表示するようにしました。<hr size=1>**PC 表示の文字サイズ拡大**<br>・PC のブラウザ（マウス操作・幅 1024px 以上）で、パネル・ボタン・ステータス表示などの UI の文字を従来の 1.5 倍に大きくしました。画面（エミュレーション画面）の大きさと表示倍率は変わりません。スマートフォン・タブレットの表示も変わりません。<br>・文字が大きくなった分だけ画面（エミュレーション画面）が縮んでしまわないよう、画面をブラウザの窓に合わせる自動縮小の計算を見直しました。従来より画面が大きく表示されます。<br>・右側のパネル（Options / ROM Files / Library など）の幅を広げました。 |
| 2026-09-20 | **新しい WebM7、フルリニューアル！**<br>・シミュレータとしてリニューアルしました。独立実装の互換 ROM セットを同梱しています。<br>・独立実装の互換 ROM セット（MIT License）を同梱し、既定で使用（ROM ファイル無しで起動可）<br>・ROM の供給元を ROM 1 本ごとに「同梱互換」「自分の ROM」から選択可<br>・機種ごとの配色と前面パネルの意匠に刷新。電源・リセット・起動モード・ドライブの操作を前面パネルへ一本化<br>・FM-77 を独立した機種に<br>・表示倍率トグル（Web標準 / x2 / x1 / FULL）を画面下に追加<br>・T77 の書き出しを標準的な形式に変更。無音だけの保存・末尾 1 バイト欠落・先頭ずれを修正<br>・新しいバージョンの案内と自動の読み直しを追加<br>・FM-7 系のリセット時の割り込みベクタ表の反映元を、起動モードで選ばれている側の起動 ROM に修正<br>・Sound パネルを拡張。FM 音源カードを搭載した構成と FM77AV 系では FM 音源 (OPN) の全レジスタを表示し、PSG / SSG / FM 各チャンネルの音量をメーターと数値で表示（パネルを開いている間、毎フレーム更新）<br>・キーボードマニュアルを新設。キー配置の全体図、BREAK / GRPH / カナ / HOME などの固有キーが日本語配列・英語配列のどのキーに当たるかの対応図、キーの割り当て変更の手順を図入りでまとめました（画面最下部の「キーボード」リンクから開けます） |
| 2026-08-18 | **不具合修正**<br>・FM77AV以上のOPNにおいて、OPNタイマ A / B のオーバーフローでステータスフラグ (bit0 / bit1) を立てる際、レジスタ `$27` の bit2 / bit3 (フラグ許可) でマスクするようにしました。許可ビットが 0 の間はフラグが立ちません (タイマのカウント・リロード・CSM のキーオンは従来どおり許可ビットと無関係に動作)。<br>・CSM モードの判定を `$27` の bit7-6 が正確に `10` の場合のみとし、未定義のモード `11` では CSM のキーオンが発火しないようにしました。CSM 中の Ch3 TL 反映抑止も同じ判定を使います。<hr size=1>**ヘッドレス実行に関する機能追加**<br>・ヘッドレス実行用の設定を追加しました。詳細はヘッドレスマニュアル 7.1.1 を参照してください。 |
| 2026-07-18 | **インテリジェントマウスに対応 / マウス設定の刷新**<br>・マウスの接続方法を選べるようにしました。Control パネルの「Mouse」が選択式になり、`なし` / `マウスセット（バスマウス）` / `インテリジェントマウス（ポート 1）` / `インテリジェントマウス（ポート 2）` から選べます。<br>・インテリジェントマウスはジョイスティックポート経由で接続する方式のため、<strong>FM 音源カード</strong>が必要です（搭載していない構成では選択できません）。機種・構成に応じて、選べる項目が自動で切り替わります。<br>・バスマウス（マウスセット）を全機種で利用できるようにしました。<br>・選んだマウス設定はブラウザに保存され、次回起動時に復元されます。<hr size=1>**3 分割スペースバーに対応**<br>・FM77AV のキーボード最下段にある 3 つのスペースキー（左 / 中 / 右）に対応しました。通常（ASCII）モードではいずれも空白として入力され、スキャンコードモードではそれぞれ固有のコードを返します。日本語配列では 無変換 → 左、変換 → 中、Space → 右 に割り当てています。 <hr size=1>**キーボードの機能追加**<br>・キーボードに WebM7 独自のメッセージ表示を追加しました。 |
| 2026-07-12 | **マウス対応**<br>・マウス入力に対応しました。Control パネルの「Mouse」を ON にすると、画面上でのマウス移動と左右ボタンがシミュレータへ伝わります。カーソルの移動量は表示倍率やフルスクリーンに合わせて自動調整され、画面上のポインタと概ね一致して動きます（対応していない機種では選択できません）。<hr size=1>**フルスクリーン表示の修正**<br>・フルスクリーンにしたとき画面が拡大されすぎて一部しか見えなくなる問題を修正しました。画面全体が正しく収まって表示されます。<hr size=1>**起動前の必須 ROM チェックを見直し**<br>・電源投入前の必須 ROM チェックを、機種と Boot Mode（BASIC / DOS）ごとの実機の ROM 構成に合わせて見直しました。F-BASIC ROM は本体搭載 ROM のため常に必須、FM-7 系は選択した Boot Mode に対応するブート ROM、FM77AV 系はイニシエータ ROM（ブート ROM は不要）が必須になります。ROM Files パネルのバッジとヘルプの一覧表も新しい区分に合わせて更新しました。<hr size=1>**ROM 選択の操作性改善**<br>・ROM Files パネルで個別 ROM を選ぶ際、対応ブラウザではファイル選択ダイアログの「ファイルの種類」欄に選択中のスロットの ROM 名（例: FBASIC30.ROM）が表示されるようになりました。連続で選ぶときは最後に開いたフォルダから始まります。<br>・選択したファイル名がスロットの ROM 名と異なる場合は確認を表示し、誤った欄への読み込みを防ぎます。<hr size=1>**ライセンス表記の整備**<br>・FM 音源部の由来と利用条件をライセンス文書とソース内に明記し、原典の利用条件文書を同梱しました。詳細は LICENSE-MIT.md を参照してください。<br>・商標に関する帰属表示を拡充しました。 |
| 2026-07-06 | **起動動作を実機に近づけました**<br>・Boot Mode を BASIC / DOS の 2 択（既定 BASIC）に整理しました。従来の Auto は廃止し、起動モードはディスクの有無ではなく設定で決まる、実機どおりの方式になりました（以前 Auto をお使いの設定は自動的に BASIC へ引き継がれます）。<br>・BASIC モードでは起動用 ROM が実際に動作してディスクを読み込み、起動可能なディスクがあればそこから、無ければ F-BASIC を起動します。より実機に近い起動になります（FM-7）。<br>・FM77AV 系でも Boot Mode（BASIC / DOS）の選択が起動に反映されるようになりました（選択しない場合は従来どおりの起動になります）。<br>・FM-7 の起動に BASIC ブート ROM（BOOT_BAS.ROM）が必須になりました。実機と同じく本体内蔵のブート ROM を用いて起動します。<hr size=1>**ヘッドレステストの拡充**<br>・ヘッドレステスト マニュアルに、ジョイスティックの方向・トリガをプログラムから与える手順を追記しました。ブラウザなしの自動テストでも、ジョイスティック操作を必要とするプログラムの動作を確認できます。<hr size=1>**操作性の改善**<br>・ROM Files パネルに、保存した ROM だけを消去するボタンを追加しました（ディスクや Library はそのまま残ります）。 |
| 2026-07-04 | **対応機種の追加**<br>・FM77AV20 と FM77AV20EX に対応しました。Machine セレクタから選択できます。<br>・Machine セレクタの下に、選択した機種が持つ機能（77AV機能 / 2DD / DMAC / 高速MMR / AV40機能 / 40EX機能）が一目でわかる<strong>能力バッジ</strong>を追加しました。「機種比較」から 6 機種の機能一覧表も開けます。また、選択した機種のメインRAM / VRAM 容量も表示します。<br>・FM77AV40EX の表示名を「FM77AV40EX/SX」に改めました（同一機種として扱われるため）。<hr size=1>**エミュレーション精度向上**<br>・一部のプログラムで画面の描画やアニメーションが実機より目に見えて遅くなる問題を修正しました。実機同様の速さで動作するようになります（FM-7 / FM77AV 系共通）。<br><hr size=1>**機能追加**<br>・ディスクの「高速読み込みモード」を追加しました（既定 ON）。OFF にすると、実機相当のシーク速度と、ディスク上の実際のセクタ配置に基づく回転タイミングで動作し、実機に近いタイミングでの動作確認ができます。<br>・画面下のステータス表示に「MMR」ランプを追加しました。メモリ管理機能の使用中に点灯し、高速MMRの動作中は緑色になります。また、ディスクのアクセスランプ（Drv0 / Drv1）は動作内容で色が変わります（黄＝2D 読み込み / 白＝2D DMA 転送 / 緑＝2DD 読み込み / 青＝2DD DMA 転送 / 赤＝書き込み）。<hr size=1>**動作の軽量化**<br>・画面に変化がないときは再描画を行わないようにし、CPU 負荷と消費電力を抑えました。 |
| 2026-06-27 | **不具合修正（キーボード入力）**<br>・ソフトウェアキーボード（画面上のキーボード図）からファンクションキー（PF1〜PF10）を押したとき、Ctrl + 文字キーなどの別の入力と区別できず、これらのキーを使うプログラムが正しく反応しないことがある不具合を修正しました。<br>・電源投入直後（最初のキーを押す前）にプログラムがキー入力を読み取ると、何も押していないのに特定のキーが押されたものと誤認される可能性があった点を修正しました。<br>・キーボードの CAPS / カナ LED の状態をプログラムから問い合わせたとき、実際の点灯状態を返すようにしました。<hr size=1>**不具合修正（画面表示）**<br>・FM77AV40 / FM77AV40EX で、320×200 の 4,096 色モードを使う一部のプログラムが、誤って 262,144 色モードで表示され色が崩れる不具合を修正しました。 |
| 2026-06-20 | **ドキュメント拡充**<br>・初めての方向けの<strong>チュートリアル（はじめてガイド）</strong>を新設しました。ROM の読み込みから起動、ディスク・テープ・BASIC Paste の基本操作まで、図解中心で順に説明しています。<br>・<strong>ヘッドレステスト マニュアル</strong>を新設しました。Node.js でシミュレータコアを直接動かして自動テストを書く方法をまとめています。<br>・テープ操作マニュアルを 1 冊に統合し、図解を多数追加しました。<hr size=1>**アプリ内マニュアル表示の改善**<br>・アプリ内のマニュアル表示が図（SVG）の表示に対応しました。<br>・画面下のフッターから「チュートリアル」「ヘッドレステスト」の各マニュアルを開けるようにしました。マニュアル（ヘルプ）冒頭の「関連マニュアル」からも開けます。 |
| 2026-06-07 | **不具合修正**<br>・FM77AV40EX の 640x400 画面でスクロールしたときに、画面表示が乱れる不具合を修正しました。<br>・キーボードの記号キーの配置が一部誤っていた問題を修正しました。`P` の右が `@`、その右が `[`、ホーム段に `]` が正しく並ぶようになり、これまで入力できなかった `@` が入力できるようになりました。<hr size=1>**操作性の改善**<br>・起動に必要な ROM がまだ読み込まれていないときは、Power スイッチを淡色表示にして操作できないようにし、「先に ROM を読み込んでください」と案内するようにしました。ROM を読み込むと Power スイッチが有効になります。<hr size=1>**操作性の改善 / モバイル対応**<br>・ROM Files パネルで、各 ROM に <strong>必須 / 任意</strong> のバッジを表示し、機種ごとに何を揃えれば起動できるか分かりやすくしました。Folder でまとめて選べることも明記しています。<br>・Library パネルに「ここに保管 → ドライブ / テープへ挿入」の案内を追加し、挿入ボタンに「Drive 0 / Drive 1 / Tape へ挿入」の説明を付けました。<br>・Disk Images / Tape Image パネルに「変更はブラウザ内に保持され、PC へ保存するには Save」を常時表示するようにしました。<br>・テープの録音保存ボタンを「録音 → .t77」「録音 → .wav」「録音クリア」と分かりやすい表記にし、録音内容が無いときはボタンを淡色表示にしました。<br>・主要なボタンや Boot Mode（Auto / DOS / BASIC）に日本語の説明（ツールチップ）を追加し、折りたたみセクションの開閉表示を分かりやすくしました。<br>・iPad / iPhone などのタッチ操作で、各ボタン・プルダウン・チェックボックス等を押しやすいよう余裕のあるサイズに整えました。スマートフォンの細い画面では、主要な操作ボタンをアイコン表示にして、押しやすく省スペースに表示するようにしました。<hr size=1>**画面下ステータス表示の刷新**<br>・画面下の状態表示を <strong>2 段</strong>に整理しました（1 段目＝キーボード関連、2 段目＝ドライブ・画面情報）。左寄せで桁数が変わっても表示が揺れないようにしています。<br>・ディスクのアクセス状況を <strong>Drive 1 / Drive 0</strong> のドライブ別ランプで表示し、ドライブの動作状態（トラック / セクタ / 状態）も画面下に表示するようにしました。<br>・<strong>Shift / Ctrl / GRPH</strong> の状態、入力したキーのコード、フレームレート、画面の解像度と色数を画面下に表示します。<br>・状態表示の LED 色を機種に合わせ、<strong>FM-7 は赤、FM77AV 系は黄</strong>にしました。<br>・ボリュームを <strong>スピーカーアイコン</strong>表示にしました。<br>・Disk Image パネルに、セット中ディスクの情報（2D / 2DD、シリンダ数・サイド数・トラック当たりセクタ数・セクタ当たりバイト数）を表示するようにしました。 |
| 2026-06-06 | **テープ: WAV 形式の読み込み・保存に対応**<br>・カセットテープイメージを T77 に加えて <strong>WAV 形式</strong>でも読み込めるようにしました。録音のノイズ・振幅変動・テープ速度の揺らぎ (ワウフラッター) を含む実録音にも対応するため、信号を完全に復調してクリーンな搬送波として再生成する方式と、波形を直接スライスする方式を、デコード品質を内部で検査しながら自動で切り替えます。<br>・標準ブロック以外のデータを含むテープの読み込みに対応しました。<br>・録音内容を T77 に加えて <strong>WAV 形式で保存</strong>できる <strong>Save .wav</strong> ボタンを追加しました。保存した WAV はそのまま読み戻せます。 <hr size=1>**UI改善 (操作性)**<br>・サイドパネル各セクションのボタンが小さすぎて押しにくい、画面によってはボタンが隠れて押せない (特に Disk Images / Tape Image の <strong>Eject</strong> ボタン) 問題を改善しました。ファイル選択欄とボタンを折り返し可能なレイアウトに見直し、ボタンが欄外へ押し出されて隠れないようにしました。あわせてパネル内ボタンの大きさを読みやすいサイズに統一し、スマートフォン等のタッチ操作時には、ボタンに加えてプルダウン・ラジオボタン・チェックボックス・スライダーにも十分なタップ領域 (44px) を確保するようにしました。<hr size=1>**不具合修正**<br>・スマートフォン等で仮想キーボードがページ最下部 (フッターより下) に表示されてしまい、画面のすぐ下にドックされない不具合を修正しました。仮想キーボードを画面の直下に配置し、フッター (コピーライト・各種リンク) の上に表示されるようにしました。<br>・タッチ対応端末 (タッチ液晶搭載 PC を含む) でフッターがサイドパネル内へ移動して見えなくなる場合がある不具合を修正し、フッターを常にページ下部へ表示するようにしました。<br>・Alt+Tab などでウィンドウのフォーカスが外れたとき、GRPH キーが押されたままの状態で残り、フォーカスが戻った後に通常キーがグラフィック文字になってしまう不具合を修正しました。ウィンドウのフォーカスが外れた時点で GRPH と押下中のキーを解放するようにしました (CAPS / カナ / INS のトグル状態は維持します)。あわせて Keyboard パネルのキー表示でも、フォーカス喪失時に押下中ハイライトが残らないようにしました。 <hr size=1>**空ディスク作成の改善 / HFE 対応 / 実機準拠強化**<br>・Library の「＋2D / ＋2DD」で作成する空ディスクを<strong>論理フォーマット済み</strong>で生成するようにしました。作成後すぐに DISK BASIC から `SAVE` できます (これまで必要だった手動の「論理FMT」操作は不要になりました)。<br>・**HFE イメージ (実機フロッピーエミュレータ媒体) の読み込みに対応**しました。読み込み時に MFM トラック長を規定値と照合し、規定を超える過大トラック (実機では起動できない可能性のある媒体) を警告します。<hr size=1><strong>エミュレーション精度向上 (実機準拠強化)</strong><br>・BASIC ROM オーバーレイが有効な領域 (`$8000-$FBFF`) への書き込みを実機同様に無視するようにしました。`$FD0F` で裏 RAM へ切り替えずに書き込むローダの不備を、実機同様に検出できます。<br>・FM77AV キーエンコーダのコード系切替に、`$D432` ステータス のハンドシェイク手順を要求するようにしました。<br>・BREAK キーの押下状態を `$FD04` bit 1 で常時読めるようにしました。FIRQ に加えて `$FD04` bit 1 のポーリングで BREAK を検出するプログラムに対応します。<hr size=1><strong>不具合修正</strong><br>・TXT / BAS の自動入力 (BASIC Paste) で、行数の多いプログラムを流し込むと画面がスクロールしたあたりから入力が乱れる (行頭が欠落して Syntax Error になる) 不具合を修正しました。自動入力の進行を画面の描画タイミングではなくエミュレーション内部のクロックで管理するようにし、120Hz / 144Hz など高リフレッシュレートのディスプレイでも入力ペースが一定になるようにしました。 |
| 2026-06-01 | **仮想キーボードのスマホ最適化 / キーカスタマイズ対応**<br>・スマートフォンの縦向き表示時に、カーソルキー・編集キー・テンキーをメインキーの下段にまとめて配置する専用レイアウトへ自動で切り替えるようにしました。縦長画面の幅に合わせてキーを大きく描画します。画面の回転に追従します。<br>・仮想キーボードを画面のすぐ下にドック配置し、コピーライト表記の上に表示されるようにしました。<br>・Keyboard パネルに <strong>キーカスタマイズ</strong> を追加しました。お使いの物理キーボードを配列 (日本語配列 / 英語配列) と種別 (フルキー / テンキーレス / コンパクト) で選択でき、「カスタマイズ開始」を押して割り当てたい FM-7 キーをクリックし、物理キーを押すと、そのキーが割り当てられます。割当はブラウザに保存され、「リセット」で既定に戻せます。FM-7 / FM77AV 共通の設定です。<br>・Keyboard パネルのキーボード図を <strong>クリックして入力</strong> できるようにしました (ソフトウェアキーボード)。Shift はスティッキー、GRPH は押している間のみ、CAP / カナ / CTR はトグル動作です。PC・タッチ端末共通です。<br>・BREAK キーの変更をキーカスタマイズに統合しました。キーボード図の BRK キーをクリックして物理キーを押すと設定できます (<code>Pause</code> は常に予備として有効、「リセット」で既定に復帰)。<br>・FM-7 でも漢字 ROM (KANJI.ROM) を利用できるようにしました。FM-7 モードでも KANJI.ROM をファイル指定でき、指定すると漢字を使うプログラムで漢字が表示されます (漢字ROMカード相当の任意オプション)。<hr size=1><strong>不具合修正</strong><br>・Keyboard / Palette / Sound のフローティングパネルを最初に開いたとき、縦方向が画面いっぱいに引き伸ばされて表示される不具合を修正しました。 |
| 2026-05-31 | **メディアライブラリ / 論理フォーマット / BASIC ペースト / テープ SAVE 対応 / 画面サイズ自動調整**<br>・**Library パネル**を新設しました。ディスク (D77) / テープ (T77) を<strong>ブラウザ内</strong>で複数保管・命名・複製・削除し、Drive 0 / Drive 1 / Tape へ挿入できます。「＋2D / ＋2DD」で空ディスクを新規作成できます。<br>・各ディスクに **論理FMT** ボタンを追加しました。FAT・ディレクトリを初期化し、DISK BASIC で `SAVE` 等の書き込みができる状態にします。「＋2D / ＋2DD」で作成した空ディスクは物理フォーマットのみのため、そのままでは書き込めません (残クラスタ 0 と表示されます) が、論理FMT を実行すると使用可能になります。内容を消去して空ディスクに戻す用途にも使えます。<br>・**Disk Images パネルに「Save」ボタン**を追加し、書き込み後のディスクイメージを <code>.d77</code> として書き出せるようにしました。<br>・**テープ SAVE に対応**しました。BASIC の `SAVE` / `SAVEM` による書き込みを自動録音し、Tape パネルの **Save .t77** ボタンで T77 ファイルとして保存できます (**REC clear** で録音をクリア)。詳しい手順は「T77 SAVE/LOAD 操作マニュアル」を参照してください。<br>・**BASIC Paste パネル**を新設しました。TXT / BAS ファイルやクリップボードの内容を、F-BASIC 画面へ 1 文字ずつ自動入力します (入力速度は本体処理に合わせて自動調整)。<br>・各パネル見出しに **ヘルプアイコン (?)** を追加し、該当する使い方の説明へジャンプできるようにしました。<br>・UI 全体 (スクリーン・ツールバー・サイドパネル) をブラウザウィンドウに収まるよう自動スケールするようにしました (通常はウィンドウの約 80%、デバッグパネル表示時は約 60%、ウィンドウのリサイズに追従)。CPU / Memory / Keyboard / Palette / Sound / Input のフローティングパネルの文字サイズも右側パネルと揃えました。<br>・<strong>【重要】Library への取り込み・挿入・論理FMT・複製、ドライブの抜き差し、および BASIC からのディスク書き込み (SAVE 等) は、すべてブラウザ内 (メモリおよびブラウザ保存) で完結します。Disk Images パネルの「Save」ボタンで <code>.d77</code> を書き出し、保存ダイアログで上書きを指定しない限り、お使いの PC 本体の D77 ファイルには一切変更が及びません。</strong>元のファイルを上書きしたくない場合は別名で保存してください。 |
| 2026-05-24 | **エミュレーション精度向上 (実機準拠強化)**<br>・共有 RAM (`$FC80-$FCFF`) への主 CPU アクセスをサブ CPU HALT 中のみ有効としました。デュアルポート RAM のバスアービトレーション制約を実機に合わせます。<br>・サブ CPU 側 I/O `$D400` / `$D40A` の read 戻り値を実機準拠に修正しました。`$D400` はキーコード上位ビット由来の値 (主 CPU `$FD00` と同じ)、`$D40A` は BUSY OFF 副作用のみで `$FF` を返します。<br>・FM77AV における `$FD0D` (OPN コマンドレジスタの PSG 互換ミラー) 書込時に下位 2 bit のみ有効化するようにしました。`$FD0D` 経由で本来アクセスできないコマンドを誤発行する経路を塞ぎます。<br>・`$FD30-$FD3F` 各種レジスタの readback (アナログパレットアドレス・RGB、マルチページ、TTL パレット) を実機準拠に統一しました。書込専用レジスタは `$FF` を返し、TTL パレット読戻時に上位ニブル `0xF0` を含めるようになります。<br>・サブ CPU 制御ポート `$FD05` の read を HALT 受理状態に直接連動させました。`$FD05=$80` 書込で HALT を要求した直後の BUSY=1 ポーリングが確実に成立するようになり、主 CPU 側 HALT プロトコルを使うプログラムで待ちループから抜けられない事象が解消します。<br>・PF1-PF10 (ファンクションキー F1-F10) のハードウェアスキャンコードを実機準拠の `$5D-$66` に修正しました (従来 `$57-$60`)。<br>・PF1-PF10 を 9bit FM-7 ASCII コード (`$0101-$010A`) として送出するようにしました。キーボードステータス `$FD00` bit7 が「現在のキーコードの第 9 bit」 (PF/拡張キー識別フラグ) を返す実機準拠の挙動になり、PF キーを Ctrl-A〜Ctrl-J と区別する必要があるプログラムで PF キー入力が機能するようになります。 |
| 2026-05-21 | **FM77AV40/AV40EX 互換性向上**<br>・FDC 2D/2DD ドライブモード切替 (`$FD1E`) に対応しました。AV40 以降で 2D ディスクのセクタ検索が正しく動作するようになります。<br>・DMAC 転送中は MMR セグメントを 0 (DMA 専用ウィンドウ) として扱うようにしました。DMA でディスクからメモリへ転送するプログラムの動作が改善します。<hr size=1>**エミュレーション精度向上**<br>・FDC `$FD03` bit 0 を IRQ マスクとして正しく解釈するようにしました。<br>・BEEP ポート `$FD03` の write 時 bit 解釈を実機準拠に修正しました。BEEP 音の発生条件が実機に近づきます。 |
| 2026-04-29 | **FM77AV40/AV40EX DMAC 対応**<br>・FM77AV40/AV40EX 搭載の DMAC (HD6844) に対応しました。FDC 経由のディスク I/O で DMA 転送が機能するようになります。<br>・チャネル 0 (FDC) の読み書き両方向、バーストモード、データチェイン制御、転送完了割込に対応しています。<hr size=1>**エミュレーション精度向上**<br>・MMR / TWR 有効時のメイン CPU クロック減速 (1.794 → 1.565 MHz) に対応しました。FM77AV40EX の高速 MMR モード (`$FD95` bit 3、1.565 → 2.016 MHz への引き上げ) も実装しています。<br>・スケジューライベントを μs ベースに保ち、CPU クロック変化時に VSYNC・PTM・OPN タイマ等の周期を自動再計算するようにしました。<br>・FM-7 におけるサブ CPU の CRT サイクルスチールを実装しました。アクティブスキャン中 (HBlank 以外) はサブ CPU の実効クロックが約 0.75 MHz (公称 2.0 MHz の 384/1024) に低下し、実機タイミングに近づきました。<br>・VSYNC イベントを 4-phase 化しました (V-active / V-blank front porch / VSYNC pulse / V-blank back porch を個別追跡)。`$FD12` bit 1 / `$D430` bit 7 の V-blank ステータスが実機タイミング (200ライン: 3.94ms、400ライン: 1.65ms) に準拠した値を返すようになりました。<hr size=1>**不具合修正**<br>・FM77AV で一部のディスクが起動しない不具合を修正しました。 |
| 2026-04-26 | **ディスクイメージ取扱い改善**<br>・ディスクイメージはローカルファイル本体への参照を保持し、Reset / Power Off→On のたびにファイルを読み直すようにしました。実行中にローカル側でファイルを更新した内容が、Reset / Power Off→On で確実に反映されます (Chrome / Edge / Opera)。<br>・Firefox / Safari ではブラウザの仕様上、本機能は利用できないため従来通りの動作となります。<br>・Disk Images パネルに「Clear Disk Cache」ボタンを追加しました。ブラウザに保存されたディスクイメージ・ファイル参照をまとめて消去します。 |
| 2026-04-25 | **エミュレーション精度向上**<br>・VSYNC 割込周期および垂直同期パルス幅 (510μs) を実機仕様に合わせて 2 段階イベント化しました。<br>・サブ CPU の HALT 中にメイン CPU の経過サイクルをサブ側が後追い実行していた結果、HALT 解除直後にサブがまとめて余分に動作していた不具合を修正しました。実機同様 HALT 中はサブクロック停止として扱います。<br>・メイン CPU の実効クロックを 2.0 MHz → 1.794 MHz (実機メモリ待ちを含む実効速度) に修正しました。OPN タイマ参照クロック比も CPU クロックに追従する形に整理しました。<br>・FM77AV DOS boot 時に起動しなかった一部のプログラムが起動するように修正しました。 |
| 2026-04-24 | **FM77AV40EX/SX 2面VRAM・ハードウェアウィンドウ対応**<br>・FM77AV40EX/SX の 2 面 400 ライン／262,144 色モードで使われる VRAM ブロック切替に対応しました。front / back 2 ブロックを独立して書込／表示できます。<br>・FM77AV40EX のハードウェアウィンドウ機能に対応しました。矩形領域内だけ背面ブロックを表示する合成描画が機能します。<br>・400 ライン／262K 色モードの VRAM スクロール処理を見直しました。<hr size=1>**不具合修正**<br>・262,144 色モードが正しく表示できていなかった不具合を修正しました。<br>・640x400ライン表示でハードウェアウィンドウ使用時に表示が乱れていた不具合を修正しました。 |
| 2026-04-23 | **スマートフォン・タブレット対応**<br>・Android スマートフォン/タブレット、iPhone、iPad に対応しました。画面の向き（縦/横）に応じてレイアウトが自動調整されます。<br>・仮想キーボードを搭載しました。PC向けのKeyboardパネルと同一のレイアウト（20列グリッド+テンキー）で、Shift/CAPS/GRPH/カナの押下状態に応じてキートップのCG ROMグリフが切り替わります。<br>・サイドパネルをハンバーガーメニュー方式のドロワーに変更しました。<br>・モバイルアクションバーにPower On/Offスライドスイッチ、Pause/Resume、Resetボタンを配置しました。<br>・外部キーボード接続時にも正しくタッチデバイスとして認識されます。<br>・PC向けデスクトップブラウザでの表示・操作に変更はありません。<hr size=1>**操作改善**<br>・ジョイスティックをPort 1、Port 2それぞれに接続するゲームパッドを個別に選択できるようになりました。 |
| 2026-04-20 | **Keyboardパネル キートップ表示改善**<br>・Keyboardパネルのタイプライタ系キーに英数字/記号/カナの表記を記載し、現在押下相当のポジションを白く、他は淡色で描画するようにしました。Shift/CAPS/カナ/GRPHの状態変化に追従して現在位置表示が切り替わります。<hr size=1>**FM77AV40/AV40EX対応**<br>・FM77AV40モード（640x400 8色、262,144色、448KB拡張RAM）に対応しました。<br>・FM77AV40EXモード（EXTSUB.ROM搭載機）を追加しました。拡張サブシステムROM（Type-D/E）のバンク切替に対応します。<br>・Machine選択をラジオボタンからドロップダウンに変更しました。<hr size=1>**不具合修正**<br>・FM音源搭載機にてFM-7用のプログラム実行時、PSG出力に歪みが発生していた不具合を修正しました。<br>・2026-04-18 の変更に伴うリグレッションを修正しました。<hr size=1>**UI改善**<br>・CPUパネルとMemoryパネルを分離しました。CPUパネルにはMain/Sub CPUの全レジスタ（A/B/X/Y/S/U/PC/DP/CC）を表示します。Memoryパネルには Boot RAM/MMR/TWR/MMRマップ/物理メモリマップ/State Dumpを配置しました。<br>・Paletteパネルの表示を画面モードに応じて切り替えるようにしました（Digital 8色 / 4,096色 / 262,144色）。アナログパレットは320x200/262Kモード時のみ表示されます。<br>・画面モード表示が全4モード（640x200/320x200/262K/640x400）に対応しました。<br>・ツールバーのキーコード表示横にキーボードモード（ASCII/SCAN）を表示するようにしました。<hr size=1>**操作改善**<br>・101英語キーボード向けに `Ctrl + [` で `@`、`Ctrl + /` で `_` を入力できるショートカットを追加しました。 |
| 2026-04-18 | **FM77AV40部分対応**<br>・AV40固有I/Oポートを一部実装しました。RD512(`$FD40`-`$FD4F`)、拡張MMR/CPUスピード(`$FD94`)、モードセレクト2(`$FD95`)、CRTC(`$FD96`-`$FD97`)、DMAC(`$FD98`-`$FD99`)。<br>・MMR拡張モード(`$FD94` bit7)に対応しました。ON時はバンク数が4→8、ページレジスタが6bit→8bitに拡張されます。<br>・RS-232C(`$FD0B`-`$FD0C`)書込スタブを追加しました。<hr size=1>**エミュレーション改善**<br>・FDCのタイミング定数をFM-7 の実効クロック（約 1.794 MHz）ベースに修正しました。<br>・FDC の連続セクタリードのタイミング精度を改善しました。<br>・FDCのREAD/WRITEコマンドでセクタIDのCフィールドとトラックレジスタの一致検証を追加しました(MB8877仕様準拠)。<br>・FM-7 の DOS モードで一部のディスクが起動しない不具合を修正しました。<br>・FDC Type I コマンド（RESTORE/SEEK/STEP）のタイミングを改善しました。<br>・`$FD13`サブROMバンク切替時のサブCPUリセットをデファード方式に変更しました。サブCPU HALT中はリセットを保留し、HALT解除時に実行します。HALT中のVRAM書込みが正しく反映されるようになります。 |
| 2026-04-17 | **エミュレーション改善**<br>・サブCPUのBUSYフラグ同期処理を修正し、一部のプログラムでメニュー文字が欠落・破壊される不具合を修正しました。<br>・サブCPU HALT受付およびサブROMバンク切替時にBUSYワンショットフラグが残存し、一部のプログラムでサブCPU初期化完了前にHALTされてデッドロックする不具合を修正しました。<br>・FM77AVモードからFM-7モードへ切り替えた際に、FM77AV固有の内部状態がクリアされず描画が崩れる不具合を修正しました。<br>・YM2203 (OPN) の `$FD15` コマンドポートにて `READSTAT`/`JOYSTICK`/`ADDRESS` 時のプリスケーラ自動発火に対応しました。<br>・OPN Timer A/B の周期計算をプリスケーラ連動にしました。<br>・Timer用参照クロックを 1.2288 MHz に変更し、一部のプログラムの BGM テンポおよび音階が実機と一致するようになりました。<br>・OPN BUSY 時間を修正し、FM音源を多用するプログラムの音声合成速度が実機に近づきました。<br>・CRT表示OF 時に正しく画面が黒クリアされるようになりました。<br>・FM-7で一部のプログラムがキー入力を受け付けない不具合を修正しました。<br>・F-BASICにてカナ、CAPS、GRPHが効いていなかった不具合を修正しました。<hr size=1> **操作改善**<br>・複数ファイルがまとまっている大きな一つのD77ファイルを複数ディスクとして扱えるようになりました。<br>・CPU / State パネルにて、現在のVMの詳細な情報をクリップボードへテキストとしてコピーできるようになりました。<br>・Debug パネルを **CPU / Keyboard / Palette / Sound / Input** の 5 つの独立パネルに分割し、それぞれ個別に開閉・ドラッグ可能にしました。<br>・ツールバーを 2 行構成にし、2 行目に「Debug Panels」ラベルとパネル開閉ボタンを配置しました。<br>・Debug (CPU) パネルに Active Page / Display Page / Mode を常時表示するようにしました。<br>・FM77AV 時のみ Analog Palette (4096 色) を Palette パネルに表示するようにしました。 |
| 2026-04-12 | **エミュレーション機能追加実装**<br>・MMR/TWRの実装を全面的に見直しました。<br>・FDC のマルチセクタリードと連続セクタリードのタイミング精度を改善しました。<br>・SubCPU割り込み制御（Cancel IRQ / FIRQ）を修正しました。<br>・FM-7 で FLEX 形式ディスクの起動を改善しました。<br><hr size=1>**UX改善**<br>・Boot Modeを手動選択できるようにしました（Auto / DOS / BASIC）。<br>・ディスクドライブの挿入音、イジェクト音、シーク音等を再現してみました。デフォルトではOFFにしていますので、ボリュームを適切に設定の上、ONでお楽しみください。<br>・ディスク・テープイメージをセットした際にファイル名が表示されるようになりました。 |
| 2026-04-08 | **エミュレーション機能追加実装**<br>・Script Processor Node の使用を止め、 AudioWorklet Node を利用してのFM音源シミュレーションへ移行しました。(FM77AV)<br>・FDC、キーボードに関する割り込みのタイミングに関する実装を全面的に見直しました。 |
| 2026-04-07 | **エミュレーション機能追加実装**<br>・SSGのCSMモードに対応しました。(FM77AV)<br>・漢字ROM(FM77/FM77AV相当)に対応しました。<hr size=1> **操作改善**<br>・画面右のパネル `Control`、`ROM Files`、`Disk Images`、`Tape Image` は、タイトルバーをクリックして短縮表示できる様になりました。<br> ・`Machine` `Joystick` `Cursor → Ten Key` の設定をブラウザが記憶するようになりました。<br>・前回使用したROM、Diskイメージ、及びテープイメージのパスをブラウザが記憶するようになりました。<hr size=1>**不具合修正**<br>・起動に必要な ROM の構成を見直しました。 |
| 2026-04-03 | **操作改善**<br>・Power On/OffボタンをスライダーUI式トグルスイッチに変更しました。<br> ・起動に必要な ROM の構成を見直しました。<br>・DOSモード/BASICモードの切り替えを廃止しました。ディスクイメージが入っていないとBASICで起動し、入っているとDOSモードとして起動します。|
| 2026-04-01 | **キーボード対応**<br>・JIS日本語キーボード(106/109)に対応しました。<br>・テンキーレスキーボード、コンパクトキーボード等でもテンキーの2/4/6/8を用いたプログラムを操作できるよう、カーソルキーをテンキーの2/4/6/8として代用できる設定を追加しました。<hr size=1>**不具合修正**<br>・FM-7 の BASIC モードで起動できない場合があった不具合を修正しました。<br>・FM77AV BASICモードで起動直後にキーボードから正しい入力が出来ない不具合を修正しました。<br>・FM77AV BASICモードで画面最下行に到達した際にスクロールしない不具合を修正しました。<br>・BEEP音の音程を実機に近づけました。<hr size=1>**そのほか**<br>・ROM Info でサイズとハッシュ値を確認できるようにしました。<br>・画面をPNGで保存するボタンを付けました。640x200モードは縦に伸ばして640x400に、320x200モードは縦横倍に伸ばして同じく640x400として保存されます。 |
| 2026-03-30 | **起動UI見直し**<br>・Start/StopをPower On/Power Offへ変更し、Reset、及びPower On/Off の挙動を実機に合わせました。<br>・デバッグパネルは起動時はOffとなるようにしました。<hr size=1>**サポートブラウザ**<br>以下のブラウザで動作確認しました<br>・Firefox 149.0 (Windows/64bit)<br>・Edge バージョン 146.0.3856.84 (公式ビルド/Windows/64bit)<br>・Chrome バージョン 146.0.7680.165（公式ビルド/Windows/64bit）<br>・Safari バージョン26.3.1 (21623.2.7.11.7)<hr size=1>**不具合修正**<br>・BASIC モード起動時の I/O ポートの初期値を見直しました。 |
| 2026-03-29 | **FM-7モード見直し**<br>・FM-7において必要なROMの見直しを行いました。<br>・FM-7モードでFM音源をOn/Off出来るようにしました。<br>・ジョイスティックをPort-1とPort-2のどちらへつなげるか選択できるようにしました。<hr size=1>**不具合修正**<br>・LOADMで動かなかった一部のプログラムに対応。<br>・一部のプログラムで SCREEN が切り替わらない不具合を修正しました。 |
| 2026-03-28 | **F-BASIC挙動とテープ読み込み改善**<br>F-BASICでINSキーの動作がおかしかった不具合を修正 |
| 2026-03-24 | **F-BASIC動作改善・テープ対応**<br>・キーボード入力修正（ASCIIモード、デフォルト小文字、Shift大文字、キーリピート対応）<br>・INS/CAPS/カナ LEDインジケーター追加 <br>・T77カセットテープイメージ読み込み対応 |
| 2026-03-22 | **FM77AVに部分的に対応**<br>・320×200 4096色モード、アナログパレット、ALU/ライン描画エンジン <br>・OPN FM音源（YM2203）<br>・スキャンコードキーボード <br>ジョイスティック選択対応。デバッグパネルを独立させてドラッグ移動可能ポップアップできるようにした。 |
| 2026-03-20 | **最初のリリース/FM-7対応**<br>・デュアルMC6809 CPU、サブシステムでメモリ共有、640×200 8色、カラーパレット、PSG音源（AY-3-8910）、FDCMB8877、キーボード、BREAKキー割り込み<br>・FM音源カード搭載時のゲームパッド対応（FM音源には未対応） |
